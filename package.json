{
    "name": "spam-detection-engine",
    "private": true,
    "type": "module",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "npm run -w @plebbit/spam-detection-shared build && npm run -w @plebbit/spam-detection-server build && npm run -w @plebbit/spam-detection-challenge build",
        "format": "prettier --write .",
        "format:check": "prettier --check .",
        "prepare": "husky"
    },
    "devDependencies": {
        "husky": "^9.1.7",
        "prettier": "^3.8.0",
        "typescript": "5.9.3"
    }
}
